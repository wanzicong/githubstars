---
trigger: always_on
---
# 开发工作流程

> 本文档在 [common/git-workflow.md](git-workflow.md) 的基础上进行了扩展，涵盖了在 git 操作之前发生的完整功能开发过程。

功能实现工作流描述了开发流水线：研究、规划、TDD、代码审查，然后提交到 git。

## 七阶段开发工作流

所有功能开发必须遵循以下结构化流程，不得跳过任何阶段。

### 第一阶段：发现（Discovery）
**目标**：理解要构建什么。

1. 创建包含全部阶段的待办事项清单
2. 若需求存在歧义，向用户澄清：正在解决什么问题？该功能应该做什么？有何约束或要求？
3. 总结理解并与用户确认

### 第二阶段：代码库探索（Codebase Exploration）
**目标**：深入理解相关代码和架构模式。

⚠️ **必须执行完整调用链追踪（参见 [dependency-management.md](dependency-management.md)），不得只看入口方法。**

1. 并行启动 2-3 个探索代理，各自关注不同维度：类似功能实现追踪、高层架构与抽象映射、UI 模式/测试方法
2. 每个代理返回 5-10 个关键文件待读清单
3. 阅读所有代理识别的文件，建立完整上下文
4. 绘制调用链路图（方法级），确保理解每个节点
5. 输出发现总结和模式分析

### 第三阶段：澄清问题（Clarification）
**目标**：在设计前消除所有歧义。

⚠️ **此为最关键的阶段，绝对不可跳过。**

识别并提问以下未明确的事项：边界情况处理、错误处理策略、集成点与接口、范围边界、设计偏好、向后兼容性要求、性能需求。

在获得用户明确答复之前，**不得进入架构设计阶段**。

### 第四阶段：架构设计（Architecture Design）
**目标**：提供多种具有不同权衡的实现方案。

1. 并行启动 2-3 个架构师代理，各侧重不同策略：
   * **最小变更方案** — 改动最小，最大化复用
   * **清晰架构方案** — 可维护性优先，优雅抽象
   * **务实平衡方案** — 速度与质量兼得
2. 审查全部方案，形成推荐意见
3. 向用户呈现：每个方案的总结、权衡对比、推荐理由
4. 等待用户明确批准后进入实施阶段

### 第五阶段：实施（Implementation）
**目标**：按选定架构构建功能。

* 严格遵守代码库既有约定和命名规范
* 代码清晰、文档完善、注释合理
* 实时更新待办事项进度

### 第六阶段：质量审查（Quality Review）
**目标**：确保代码正确、简洁、可维护。

1. 并行启动 3 个审查代理，各侧重不同维度：简洁性/不重复/优雅性、错误检测/功能正确性、项目约定/抽象层次
2. 整合发现结果，筛选置信度 ≥ 80 的问题（参见 [code-review.md](code-review.md) 置信度评分标准）
3. 向用户呈现发现，由用户决定处理方式

### 第七阶段：总结（Summary）
**目标**：记录已完成工作。

输出：构建了什么功能、做出的关键决策及理由、修改了哪些文件（含路径）、建议的后续步骤。

## 功能实现工作流程（简化版）

0. **研究与复用** *(任何新实现前必须执行)*
   * **优先进行 GitHub 代码搜索：** 在编写任何新代码之前，先运行 `gh search repos` 和 `gh search code` 以查找现有的实现、模板和模式。
   * **其次查阅库文档：** 在实现之前，使用 Context7 或主要供应商文档来确认 API 行为、包的使用以及版本特定的细节。
   * **仅在以上两者不足时使用 Exa：** 在 GitHub 搜索和主要文档之后，再使用 Exa 进行更广泛的网络研究或探索。
   * **检查包注册中心：** 在编写工具代码之前，先搜索 npm、PyPI、crates.io 和其他注册中心。优先选择经过实战检验的库，而不是自己动手实现。
   * **寻找可适配的实现：** 寻找能解决 80% 以上问题的开源项目，以便进行分叉、移植或封装。
   * 如果经过验证的方法能满足需求，优先采用或移植该方法，而不是编写全新的代码。

1. **先规划**
   * 使用 **planner** 智能体来创建实施计划
   * 编码前生成规划文档：PRD、架构、系统设计、技术文档、任务列表
   * 识别依赖项和风险
   * 分解为多个阶段

2. **TDD 方法**
   * 使用 **tdd-guide** 智能体
   * 先编写测试（RED）
   * 实现代码以通过测试（GREEN）
   * 重构（IMPROVE）
   * 验证 80% 以上的覆盖率

3. **代码审查**
   * 编写代码后立即使用 **code-reviewer** 智能体
   * 解决 CRITICAL 和 HIGH 级别的问题
   * 尽可能修复 MEDIUM 级别的问题

4. **提交与推送**
   * 详细的提交信息
   * 遵循约定式提交格式
   * 提交信息格式和 PR 流程请参阅 [git-workflow.md](git-workflow.md)

## 与其他规则的集成

此规则与以下规则配合：

- [coding-style.md](coding-style.md) - 多角色全局思考、存量逻辑感知
- [dependency-management.md](dependency-management.md) - 调用链追踪与依赖管理
- [code-review.md](code-review.md) - 置信度评分标准
- [git-workflow.md](git-workflow.md) - 提交与 PR 流程
- [agents.md](agents.md) - 代理编排与使用

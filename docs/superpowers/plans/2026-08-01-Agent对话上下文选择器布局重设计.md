# Agent 对话上下文选择器布局重设计 —— 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 AgentChat 页的上下文选择器（仓库/分类选择）重构为「一体化输入框 + 紧凑单行列表」，修复上下文行独占一行、Tag 无限换行、弹层列表左对齐缺失三个布局问题。

**Architecture:** 重写 `ContextPicker.tsx` 为带 chip 收纳 + 弹层的组件，并把 `AgentChat/index.tsx` 输入区改造为单圆角容器（chip 区 + TextArea + 右下按钮行），发送/停止按钮并入输入区。组件对外接口（`value`/`onChange`）与发送逻辑保持不变。

**Tech Stack:** React 19 + Ant Design 6 + TypeScript + Vitest + Testing Library。

**设计规格：** [docs/superpowers/specs/2026-08-01-Agent对话上下文选择器布局重设计.md](../specs/2026-08-01-Agent对话上下文选择器布局重设计.md)

## Global Constraints

- 所有验证命令必须先 `cd packages/frontend` 再执行（P1 验证路径确认）。
- 构建验证必须用 `npm run build`（`tsc -b && vite build`），不得只用 `tsc --noEmit`（P0）。
- 代码禁止显式 `any`（catch 用 `unknown` + `instanceof` 收窄）；禁止嵌套三元；禁止 `.match()` 用 `.exec()`；数组判空用 `> 0`（P0 sonarjs 规则）。
- 数字参数禁止全局 `parseInt()`，用 `Number.parseInt()`；Promise 禁止 `.catch()` 链，用 `try/await/catch`（用户全局硬约束）。
- 同目录禁止同名异缀文件（如 `helpers.ts` + `helpers.tsx`）（P0）。
- 单行解构/属性赋值给非空类型前，源类型含 `| null` 时必须 `?? ''` 兜底（P0 null 安全）。
- 组件文件 ≤ 300 行，超过必须拆子组件；函数 Cognitive Complexity ≤ 15（P0/P1）。
- Ant Design 6 API：不使用已废弃 props（如 Alert `message`）；使用组件前确认当前版本签名（P2）。
- 日志只加 `info` / `error` 类型。
- 提交信息结尾带 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。

## 文件结构

| 文件 | 职责 | 动作 |
|------|------|------|
| `packages/frontend/src/pages/AgentChat/ContextPicker.tsx` | 上下文选择器：chip 收纳区 + 弹层（仓库紧凑单行列表 / 分类树） | 重写 |
| `packages/frontend/src/pages/AgentChat/index.tsx` | 输入区改造为单容器，集成 ContextPicker + TextArea + 圆形发送/停止钮 | 局部修改（约 1719-1767 行） |
| `packages/frontend/test/components/ContextPicker.test.tsx` | ContextPicker 单元测试 | 新建 |

**预估行数**：ContextPicker.tsx 重写后约 290 行（< 300 红线，无需拆分；实现时若超出，把 `RepoRow` 行组件拆到 `ContextPickerRepoRow.tsx`，禁止同名异缀）。

**复用要点：**
- `GithubRepo.starsCount: number`（`packages/frontend/src/types/index.ts`），star 数列用 `formatNumberShort()`（`packages/frontend/src/utils/format.ts:111`，`1049→"1.0K"`、`61217→"61.2K"`）。
- 分类树勾选联动逻辑 `flattenCategories` / `handleCheckCategory` 从现版 ContextPicker 原样保留。
- 仓库搜索防抖（300ms）与分类树一次性加载逻辑原样保留。

---

### Task 1: ContextPicker 重写（chip 收纳 + 弹层紧凑单行列表）

**Files:**
- Modify: `packages/frontend/src/pages/AgentChat/ContextPicker.tsx`（整体重写）
- Test: `packages/frontend/test/components/ContextPicker.test.tsx`

**Interfaces:**
- Consumes: `fetchStarList({ page, size, keyword? })` → `{ records: GithubRepo[] }`；`fetchCategoryTree()` → `CategoryNode[]`；`GithubRepo`（`id`, `fullName`, `starsCount`）；`CategoryNode`（`id`, `name`, `repoCount`, `children?`）；`formatNumberShort(n: number): string`。
- Produces: `export interface ChatContextItem { type: 'repo' | 'category'; id: number; label: string }`（不变）；`export default function ContextPicker({ value, onChange }: { value: ChatContextItem[]; onChange: (items: ChatContextItem[]) => void })`（签名不变）。Task 2 以 `<ContextPicker value onChange />` 使用，无任何新 props。

- [ ] **Step 1: 写失败测试**

新建 `packages/frontend/test/components/ContextPicker.test.tsx`：

```tsx
/**
 * ContextPicker 单元测试 —— 一体化输入框版
 * 覆盖：chip 展示/折叠/删除、弹层仓库列表勾选与对齐结构、已选行清空、分类树勾选
 */
import React from 'react'
import { render, screen, waitFor, within } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { App as AntApp, ConfigProvider } from 'antd'
import { beforeEach, describe, expect, it, vi } from 'vitest'
import ContextPicker, { type ChatContextItem } from '../../src/pages/AgentChat/ContextPicker'

const { fetchStarList, fetchCategoryTree } = vi.hoisted(() => ({
    fetchStarList: vi.fn(),
    fetchCategoryTree: vi.fn(),
}))

vi.mock('@/api', () => ({ fetchStarList, fetchCategoryTree }))

const REPO = (id: number, fullName: string, starsCount = 1000) => ({
    id, repoName: fullName.split('/')[1], fullName,
    description: null, descriptionCn: null, readmeCn: null, readmeOriginal: null,
    readmeFetched: false, language: null, ownerName: fullName.split('/')[0],
    ownerAvatarUrl: '', htmlUrl: '', homepage: null, starsCount,
    forksCount: 0, watchersCount: 0, openIssuesCount: 0, topics: null,
    licenseName: null, isFork: false, isArchived: false, repoSize: null,
    defaultBranch: null, visibility: null, repoCreatedAt: null, repoUpdatedAt: null,
    repoPushedAt: null, starredAt: null,
})

function renderPicker(value: ChatContextItem[] = [], onChange = vi.fn()) {
    render(
        <ConfigProvider>
            <AntApp>
                <ContextPicker value={value} onChange={onChange} />
            </AntApp>
        </ConfigProvider>,
    )
    return onChange
}

describe('ContextPicker', () => {
    beforeEach(() => {
        vi.clearAllMocks()
        fetchStarList.mockResolvedValue({ records: [REPO(1, 'facebook/react', 234000), REPO(2, 'vuejs/core', 48000)] })
        fetchCategoryTree.mockResolvedValue([{ id: 10, name: '前端框架', repoCount: 12, children: [] }])
    })

    it('未选上下文时只渲染「＋」按钮，无 chip 区', () => {
        renderPicker([])
        expect(screen.getByRole('button', { name: /plus/i })).toBeInTheDocument()
        expect(screen.queryByText('＋')).toBeNull()
    })

    it('选中 3 个以内 chip 全部展示', () => {
        const items: ChatContextItem[] = [
            { type: 'repo', id: 1, label: 'facebook/react' },
            { type: 'repo', id: 2, label: 'vuejs/core' },
            { type: 'category', id: 10, label: '前端框架' },
        ]
        renderPicker(items)
        expect(screen.getByText('facebook/react')).toBeInTheDocument()
        expect(screen.getByText('vuejs/core')).toBeInTheDocument()
        expect(screen.getByText('前端框架')).toBeInTheDocument()
        expect(screen.queryByText(/^\+\d+$/)).toBeNull()
    })

    it('选中超过 3 个时折叠为 3 个 + +N', () => {
        const items: ChatContextItem[] = [
            { type: 'repo', id: 1, label: 'a/one' },
            { type: 'repo', id: 2, label: 'b/two' },
            { type: 'repo', id: 3, label: 'c/three' },
            { type: 'repo', id: 4, label: 'd/four' },
            { type: 'repo', id: 5, label: 'e/five' },
        ]
        renderPicker(items)
        expect(screen.getByText('a/one')).toBeInTheDocument()
        expect(screen.getByText('c/three')).toBeInTheDocument()
        expect(screen.queryByText('d/four')).toBeNull()
        expect(screen.getByText('+2')).toBeInTheDocument()
    })

    it('chip 删除调用 onChange 移除该项', async () => {
        const items: ChatContextItem[] = [{ type: 'repo', id: 1, label: 'facebook/react' }]
        const onChange = renderPicker(items)
        await userEvent.click(screen.getByRole('button', { name: /close/i }))
        expect(onChange).toHaveBeenCalledWith([])
    })

    it('打开弹层展示仓库列表并可选中', async () => {
        const onChange = renderPicker([])
        await userEvent.click(screen.getByRole('button', { name: /plus/i }))
        const row = await screen.findByText('facebook/react')
        await userEvent.click(row)
        expect(onChange).toHaveBeenCalledWith([{ type: 'repo', id: 1, label: 'facebook/react' }])
    })

    it('弹层显示 star 数（formatNumberShort 格式）', async () => {
        renderPicker([])
        await userEvent.click(screen.getByRole('button', { name: /plus/i }))
        await screen.findByText('facebook/react')
        expect(screen.getByText('234.0K')).toBeInTheDocument()
    })

    it('已有选中项时弹层显示「已选 n 项」，清空按钮移除全部', async () => {
        const items: ChatContextItem[] = [{ type: 'repo', id: 1, label: 'facebook/react' }]
        const onChange = renderPicker(items)
        await userEvent.click(screen.getByRole('button', { name: /plus/i }))
        expect(await screen.findByText('已选 1 项')).toBeInTheDocument()
        await userEvent.click(screen.getByText('清空'))
        expect(onChange).toHaveBeenCalledWith([])
    })

    it('分类 Tab 勾选分类', async () => {
        const onChange = renderPicker([])
        await userEvent.click(screen.getByRole('button', { name: /plus/i }))
        await userEvent.click(await screen.findByRole('tab', { name: '分类' }))
        const tree = await screen.findByRole('tree')
        await userEvent.click(within(tree).getByText(/前端框架/))
        await waitFor(() => {
            expect(onChange).toHaveBeenCalledWith([{ type: 'category', id: 10, label: '前端框架' }])
        })
    })
})
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd packages/frontend && npx vitest run test/components/ContextPicker.test.tsx
```

预期：FAIL（组件尚无 chip 折叠、紧凑单行列表、「已选 n 项」等新结构，多个用例失败）。

- [ ] **Step 3: 重写 ContextPicker.tsx**

整体重写 `packages/frontend/src/pages/AgentChat/ContextPicker.tsx` 为以下实现：

```tsx
import { useState, useEffect, useCallback } from 'react'
import { Popover, Input, Tabs, Tag, Tree, Spin, Empty, Button, Checkbox, Tooltip, theme } from 'antd'
import { PlusOutlined, StarOutlined, BranchesOutlined, CloseOutlined } from '@ant-design/icons'
import { fetchStarList, fetchCategoryTree } from '@/api'
import { formatNumberShort } from '@/utils/format'
import type { GithubRepo, CategoryNode } from '@/types'

/** 选中的上下文项（仓库或分类） */
export interface ChatContextItem {
    type: 'repo' | 'category'
    id: number
    /** 展示名（仓库 fullName / 分类 name） */
    label: string
}

interface Props {
    /** 当前已选中的上下文项 */
    value: ChatContextItem[]
    onChange: (items: ChatContextItem[]) => void
}

/** chip 区最多完整展示的个数，超出折叠为 +N */
const MAX_VISIBLE_CHIPS = 3

/** 把分类树拍平为一二级选项（含每级名称） */
function flattenCategories(nodes: CategoryNode[]): CategoryNode[] {
    const result: CategoryNode[] = []
    const walk = (list: CategoryNode[]) => {
        for (const node of list) {
            result.push(node)
            if (node.children && node.children.length > 0) walk(node.children)
        }
    }
    walk(nodes)
    return result
}

/**
 * 对话上下文选择器 —— 「＋」按钮 + 弹层 + 已选 chip 收纳区。
 * chip 区仅在有选中项时渲染，最多展示 MAX_VISIBLE_CHIPS 个，超出折叠为 +N；
 * 弹层内仓库列表为紧凑单行四列网格（复选框/星标/名称/star数），保证名称列左边缘严格对齐。
 */
export default function ContextPicker({ value, onChange }: Props) {
    const { token } = theme.useToken()
    const [open, setOpen] = useState(false)
    const [repoKeyword, setRepoKeyword] = useState('')
    const [repoOptions, setRepoOptions] = useState<GithubRepo[]>([])
    const [repoLoading, setRepoLoading] = useState(false)
    const [categories, setCategories] = useState<CategoryNode[]>([])
    const [catLoading, setCatLoading] = useState(false)

    // 弹层打开时加载分类树（一次性）
    useEffect(() => {
        if (!open || categories.length > 0) return
        const load = async () => {
            setCatLoading(true)
            try {
                const tree = await fetchCategoryTree()
                setCategories(tree)
            } catch {
                // 分类加载失败不阻断仓库搜索
            } finally {
                setCatLoading(false)
            }
        }
        void load()
    }, [open, categories.length])

    // 仓库搜索（防抖 300ms）
    useEffect(() => {
        const keyword = repoKeyword.trim()
        const timer = setTimeout(async () => {
            setRepoLoading(true)
            try {
                const result = await fetchStarList({ page: 1, size: 20, keyword: keyword || undefined })
                setRepoOptions(result.records)
            } catch {
                setRepoOptions([])
            } finally {
                setRepoLoading(false)
            }
        }, 300)
        return () => clearTimeout(timer)
    }, [repoKeyword])

    const isSelected = useCallback(
        (type: ChatContextItem['type'], id: number) => value.some((item) => item.type === type && item.id === id),
        [value],
    )

    const toggleItem = useCallback(
        (item: ChatContextItem) => {
            if (isSelected(item.type, item.id)) {
                onChange(value.filter((v) => !(v.type === item.type && v.id === item.id)))
            } else {
                onChange([...value, item])
            }
        },
        [value, onChange, isSelected],
    )

    const removeItem = useCallback(
        (item: ChatContextItem) => {
            onChange(value.filter((v) => !(v.type === item.type && v.id === item.id)))
        },
        [value, onChange],
    )

    // 分类树数据（勾选联动选中）
    const treeData = categories.map((node) => ({
        title: `${node.name}（${node.repoCount}）`,
        key: node.id,
        children: node.children?.map((child) => ({
            title: `${child.name}（${child.repoCount}）`,
            key: child.id,
        })),
    }))
    const checkedCatKeys = value.filter((v) => v.type === 'category').map((v) => v.id)

    const handleCheckCategory = (keys: React.Key[]) => {
        const flat = flattenCategories(categories)
        const selectedCats: ChatContextItem[] = []
        for (const key of keys) {
            const node = flat.find((n) => n.id === Number(key))
            if (node) selectedCats.push({ type: 'category', id: node.id, label: node.name })
        }
        onChange([...value.filter((v) => v.type === 'repo'), ...selectedCats])
    }

    // ── 仓库紧凑单行列表：复选框列(16px) + 星标列(16px) + 名称列(flex:1) + star数列(48px) ──
    const renderRepoRow = (repo: GithubRepo) => {
        const selected = isSelected('repo', repo.id)
        return (
            <div
                key={repo.id}
                onClick={() => toggleItem({ type: 'repo', id: repo.id, label: repo.fullName })}
                style={{
                    display: 'flex',
                    alignItems: 'center',
                    gap: 8,
                    height: 32,
                    padding: '0 10px',
                    cursor: 'pointer',
                    borderBottom: `1px solid ${token.colorBorderSecondary}`,
                    background: selected ? token.colorPrimaryBg : 'transparent',
                }}
            >
                <span style={{ width: 16, flexShrink: 0, display: 'inline-flex', justifyContent: 'center' }}>
                    <Checkbox checked={selected} style={{ pointerEvents: 'none' }} />
                </span>
                <span style={{ width: 16, flexShrink: 0, display: 'inline-flex', justifyContent: 'center' }}>
                    <StarOutlined style={{ color: '#faad14', fontSize: 12 }} />
                </span>
                <Tooltip title={repo.fullName} placement='topLeft'>
                    <span style={{ flex: 1, minWidth: 0, fontSize: 13, overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>
                        {repo.fullName}
                    </span>
                </Tooltip>
                <span style={{ width: 48, flexShrink: 0, textAlign: 'right', fontSize: 11, color: token.colorTextTertiary }}>
                    {formatNumberShort(repo.starsCount)}
                </span>
            </div>
        )
    }

    const repoTab = (
        <div>
            <Input.Search
                size='small'
                placeholder='搜索仓库名/描述…'
                value={repoKeyword}
                onChange={(e) => setRepoKeyword(e.target.value)}
                style={{ marginBottom: 8 }}
                allowClear
            />
            <Spin spinning={repoLoading}>
                {repoOptions.length === 0 ? (
                    <Empty image={Empty.PRESENTED_IMAGE_SIMPLE} description='无匹配仓库' style={{ margin: '16px 0' }} />
                ) : (
                    <div style={{ maxHeight: 320, overflowY: 'auto' }}>{repoOptions.map(renderRepoRow)}</div>
                )}
            </Spin>
        </div>
    )

    const categoryTab = (
        <Spin spinning={catLoading}>
            {treeData.length === 0 ? (
                <Empty image={Empty.PRESENTED_IMAGE_SIMPLE} description='暂无分类' style={{ margin: '16px 0' }} />
            ) : (
                <div style={{ maxHeight: 320, overflowY: 'auto' }}>
                    <Tree
                        checkable
                        defaultExpandAll
                        treeData={treeData}
                        checkedKeys={checkedCatKeys}
                        onCheck={(keys) => handleCheckCategory(keys as React.Key[])}
                    />
                </div>
            )}
        </Spin>
    )

    const popoverContent = (
        <div style={{ width: 360 }}>
            {value.length > 0 && (
                <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 4 }}>
                    <span style={{ fontSize: 12, color: token.colorTextSecondary }}>已选 {value.length} 项</span>
                    <Button type='link' size='small' style={{ padding: 0, fontSize: 12 }} onClick={() => onChange([])}>
                        清空
                    </Button>
                </div>
            )}
            <Tabs
                size='small'
                items={[
                    { key: 'repo', label: '仓库', children: repoTab },
                    { key: 'category', label: '分类', children: categoryTab },
                ]}
            />
        </div>
    )

    // ── chip 收纳区：仅选中时渲染，最多 MAX_VISIBLE_CHIPS 个，超出折叠 +N ──
    const visibleChips = value.slice(0, MAX_VISIBLE_CHIPS)
    const hiddenCount = value.length - visibleChips.length

    return (
        <div style={{ display: 'flex', alignItems: 'center', flexWrap: 'wrap', gap: 6 }}>
            <Popover
                content={popoverContent}
                trigger='click'
                open={open}
                onOpenChange={setOpen}
                placement='topRight'
                arrow={false}
            >
                <Tooltip title='添加上下文'>
                    <Button size='small' type='text' icon={<PlusOutlined />} aria-label='添加上下文' style={{ color: token.colorTextSecondary }} />
                </Tooltip>
            </Popover>
            {visibleChips.map((item) => (
                <Tag
                    key={`${item.type}-${item.id}`}
                    icon={item.type === 'repo' ? <StarOutlined /> : <BranchesOutlined />}
                    closeIcon={<CloseOutlined />}
                    onClose={() => removeItem(item)}
                    color={item.type === 'repo' ? 'gold' : 'blue'}
                    style={{ margin: 0, fontSize: 12, maxWidth: 180, overflow: 'hidden', textOverflow: 'ellipsis' }}
                >
                    {item.label}
                </Tag>
            ))}
            {hiddenCount > 0 && (
                <Button type='text' size='small' style={{ fontSize: 12, color: token.colorTextSecondary, padding: '0 4px' }} onClick={() => setOpen(true)}>
                    +{hiddenCount}
                </Button>
            )}
        </div>
    )
}
```

要点（对照 Global Constraints 逐条满足）：
- `Tag` 加 `maxWidth: 180` + `ellipsis` 防超长仓库名撑破 chip 区（视觉安全）。
- `+N` 为可点击文字按钮，点击打开弹层。
- 「＋」为图标按钮 + Tooltip，带 `aria-label='添加上下文'` 便于测试定位。
- 复选框用 `pointerEvents: 'none'`，点击事件统一走整行 `onClick`，避免双重触发。
- 无 `any`、无嵌套三元、无 `.match()`；`repoOptions.length === 0` 判空正确。

- [ ] **Step 4: 运行测试确认通过**

```bash
cd packages/frontend && npx vitest run test/components/ContextPicker.test.tsx
```

预期：PASS（9 个用例全绿）。若「star 数」用例失败，确认 `formatNumberShort(234000)` 实际输出（`234.0K`）并同步断言。

- [ ] **Step 5: Lint + 构建验证**

```bash
cd packages/frontend && npm run lint 2>&1 | grep -E "error|sonarjs" ; npm run build 2>&1 | grep -E "ContextPicker|error TS" ; echo "exit=$?"
```

预期：无 ContextPicker 相关 error；`npm run build` 零 TS error。

- [ ] **Step 6: Commit**

```bash
git add packages/frontend/src/pages/AgentChat/ContextPicker.tsx packages/frontend/test/components/ContextPicker.test.tsx
git commit -m "feat(agent): 重写上下文选择器为 chip 收纳 + 紧凑单行列表弹层

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: AgentChat 输入区改造为一体化容器

**Files:**
- Modify: `packages/frontend/src/pages/AgentChat/index.tsx:1719-1767`（输入区 JSX）

**Interfaces:**
- Consumes: Task 1 的 `ContextPicker`（`value`/`onChange` 不变）；同文件已有的 `draftInput`、`setDraftInput`、`inputRef`、`handleKeyDown`、`handleSend`、`handleStop`、`loading`、`isMobile`、`token`（`theme.useToken()`）、`contextItems`、`setContextItems`，以及已导入的 `SendOutlined`、`StopOutlined` 图标。
- Produces: 无新导出。输入区改为单圆角容器，原独立「发送/停止」矩形按钮删除，改为容器内右下圆形图标按钮。

- [ ] **Step 1: 定位并通读现有输入区**

读取 `packages/frontend/src/pages/AgentChat/index.tsx` 第 1719-1767 行（`{/* ── INPUT ... */}` 注释到 `</Flex>` 结束），确认与计划基线一致（ContextPicker 独立一行在 TextArea 上方、发送/停止为矩形按钮）。同时确认该文件顶部已从 `@ant-design/icons` 导入 `SendOutlined`、`StopOutlined`（现版已有，无需新增导入）。

- [ ] **Step 2: 改造输入区 JSX**

把第 1729-1767 行的 `<Flex vertical gap={6} ...>...</Flex>` 整体替换为：

```tsx
          <Flex vertical gap={6} style={{ maxWidth: isMobile ? '100%' : '80%', margin: '0 auto', width: '100%' }}>
            {/* 一体化输入容器：已选上下文 chip + TextArea + 右下「＋」/圆形发送钮 */}
            <div
              style={{
                border: `1px solid ${token.colorBorderSecondary}`,
                borderRadius: 12,
                background: token.colorFillAlter,
                padding: '8px 12px 6px',
              }}
            >
              <ContextPicker value={contextItems} onChange={setContextItems} />
              <Input.TextArea
                ref={inputRef as React.Ref<React.ComponentRef<typeof Input.TextArea>>}
                value={draftInput}
                onChange={(e) => setDraftInput(e.target.value)}
                onKeyDown={handleKeyDown}
                placeholder="输入你想查询的 GitHub 仓库或问题…"
                autoSize={{ minRows: 1, maxRows: 4 }}
                variant="borderless"
                style={{ padding: 0, fontSize: 14, background: 'transparent', boxShadow: 'none' }}
              />
              <Flex justify="flex-end" align="center" gap={8} style={{ marginTop: 2 }}>
                {loading ? (
                  <Button
                    danger
                    type="primary"
                    shape="circle"
                    size="small"
                    icon={<StopOutlined />}
                    onClick={handleStop}
                    aria-label="停止"
                  />
                ) : (
                  <Button
                    type="primary"
                    shape="circle"
                    size="small"
                    icon={<SendOutlined />}
                    onClick={handleSend}
                    disabled={!draftInput.trim()}
                    aria-label="发送"
                  />
                )}
              </Flex>
            </div>
            <Text type="secondary" style={{ fontSize: 11, textAlign: 'center' }}>
              Enter 发送 · Shift+Enter 换行 · 基于 Claude Agent SDK + GitHub MCP
            </Text>
          </Flex>
```

改动说明：
- `ContextPicker` 从独立一行挪进容器内顶部（chip 区无选中项时不占位）。
- TextArea 由 `variant="filled"` 改 `variant="borderless"`，去掉自身背景与圆角，融入容器（Ant Design 6 用 `variant` 而非废弃的 `bordered`）。
- 原矩形「发送/停止」按钮删除，改为容器内右下角 `shape="circle" size="small"` 图标按钮；`loading` 态为红色停止钮，逻辑不变。
- 底部提示行保留在容器外。
- `aria-label` 用于测试与可访问性。

- [ ] **Step 3: Lint + 类型 + 构建验证**

```bash
cd packages/frontend && npm run lint 2>&1 | grep -E "error|sonarjs" ; npm run build 2>&1 | grep -E "AgentChat|error TS" ; echo "done"
```

预期：无 AgentChat 相关 error；`npm run build` 零 TS error。若报「`SendOutlined`/`StopOutlined` 未使用/未定义」或 Flex/Text 未导入，按提示在文件顶部 import 区补齐/清理。

- [ ] **Step 4: 运行相关测试套件**

```bash
cd packages/frontend && npx vitest run test/components/ContextPicker.test.tsx test/regression/agent-chat-session-restore.test.tsx
```

预期：全部 PASS。回归测试确认输入区改造未破坏会话恢复逻辑。

- [ ] **Step 5: Commit**

```bash
git add packages/frontend/src/pages/AgentChat/index.tsx
git commit -m "feat(agent): 输入区改造为一体化容器，发送钮并入右下

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: 全量验证 + 浏览器实测

**Files:**
- 无代码改动；纯验证任务。

**Interfaces:**
- Consumes: Task 1 + Task 2 的全部产出。
- Produces: 验证结论（通过 / 需修复项清单）。

- [ ] **Step 1: 前端全量 lint + 构建**

```bash
cd packages/frontend && npm run lint 2>&1 | tail -20 && npm run build 2>&1 | tail -5
```

预期：lint 零 error（`sonarjs` 无新增）；build 零 TS error、Vite 构建成功。

- [ ] **Step 2: 前端全量单元测试**

```bash
cd packages/frontend && npm run test 2>&1 | tail -15
```

预期：全部测试套件 PASS（含既有回归测试）。

- [ ] **Step 3: 启动服务并浏览器实测**

确认 MySQL（`127.0.0.1:3307`）已启动；启动后端（`:10002`）与前端（`:10001`），用浏览器 MCP 打开 Agent 对话页，逐项验证：

- [ ] 无上下文：输入容器只显示输入框 + 右下「＋」/发送钮，上方无多余行
- [ ] 点「＋」打开弹层，搜索仓库并勾选 2 个 → chip 区出现 2 个 chip，弹层「已选 2 项」
- [ ] 继续选到 4-5 个 → chip 区只显示 3 个 + `+N`，点 `+N` 重新打开弹层
- [ ] 弹层仓库列表各行仓库名左边缘严格对齐；超长名省略号 + 悬停 Tooltip
- [ ] chip 上 ✕ 可单独删除；弹层「清空」移除全部
- [ ] 分类 Tab 勾选分类即时生效，缩进对齐
- [ ] 输入文字点发送（圆形钮）→ 正常发送；流式期间变为红色停止钮可停止
- [ ] 窄屏（<1024px）下容器自适应不破裂

- [ ] **Step 4: 修复实测发现的问题并复验**

按实测结果修复（若有），每处修复后重跑对应 lint/build/test，再浏览器复验。

- [ ] **Step 5: 最终提交（如有修复）**

```bash
git add -A
git commit -m "fix(agent): 上下文选择器实测问题修复

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Self-Review 记录

- **Spec 覆盖**：四节设计均有对应任务 —— 一体化输入框（Task 2）、弹层规格（Task 1）、紧凑单行列表（Task 1）、实现与影响范围（Task 1-3）。star 数列、「已选 n 项清空」均已含。✅
- **占位符扫描**：无 TBD/TODO；所有代码步骤含完整代码块。✅
- **类型一致性**：`ChatContextItem`、`ContextPicker` props（`value`/`onChange`）、`formatNumberShort`、`GithubRepo.starsCount` 在 Task 1/2 间一致；Task 2 引用的 `handleSend`/`handleStop`/`loading`/`draftInput` 等均为 AgentChat 现有符号。✅
- **风险**：低（纯前端两文件 + 一个测试文件，无 API/DB 变更）。

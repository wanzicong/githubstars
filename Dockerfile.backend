# ============================================================
# GitHub Stars — 后端 Docker 多阶段构建 (NestJS + Prisma)
# ============================================================
# 构建方式:
#   docker build -t githubstars-backend -f Dockerfile.backend .
# ============================================================
#
# 构建策略（Turborepo 标准 Docker 模式）:
#   1. pruner — COPY . . 后 turbo prune，裁剪出 backend 所需的最小文件集
#   2. installer — 先装依赖（利用 Docker layer cache），再复制源码
#   3. builder — 编译 + Prisma Client 生成 + 生产依赖裁剪
#   4. runner — 最小运行时镜像
# ============================================================

# ─── Stage 1: Turborepo Prune ──────────────────────────────
# 分析 monorepo 依赖图，裁剪出 backend 及其 workspace 依赖
FROM node:22-alpine AS pruner
WORKDIR /app

RUN npm install -g turbo@^2.5.0

# 复制整个 monorepo（.dockerignore 过滤 node_modules/dist 等）
COPY . .

# 裁剪：只保留 @githubstars/backend 及其依赖
RUN turbo prune @githubstars/backend --docker


# ─── Stage 2: 安装依赖 ─────────────────────────────────────
# 先装 package.json（Docker layer caching 优化），再复制源码
FROM node:22-alpine AS installer
WORKDIR /app

# 配置 npm 使用国内镜像源（无需代理）
ENV NPM_CONFIG_REGISTRY=https://registry.npmmirror.com

# 1️⃣ 先复制裁剪后的 package.json 层（命中缓存的概率高）
COPY --from=pruner /app/out/json/ .
COPY --from=pruner /app/out/package-lock.json ./package-lock.json

# 安装依赖（--ignore-scripts 避免因 Prisma schema 未就位而报错）
# 添加 npm 缓存清理和重试，避免 "Exit handler never called" npm bug
RUN npm cache clean --force && \
    for i in 1 2 3; do npm ci --ignore-scripts && break || sleep 5; done

# 2️⃣ 再复制完整的裁剪后源码（变化频繁，放后面）
COPY --from=pruner /app/out/full/ .

# 复制根目录共享配置文件（turbo prune 不会自动包含）
COPY --from=pruner /app/tsconfig.base.json ./tsconfig.base.json

# 生成 Prisma Client
RUN npx prisma generate --schema=packages/backend/prisma/schema.prisma


# ─── Stage 3: 编译 ─────────────────────────────────────────
FROM installer AS builder

# Turborepo 编译 backend（自动先编译 @githubstars/shared）
RUN npx turbo run build --filter=@githubstars/backend

# 保留生产依赖（Prisma 运行时 + 业务代码）
RUN npm prune --omit=dev

# 验证 Prisma Client
RUN node -e "require('@prisma/client')" && echo "Prisma Client OK"


# ─── Stage 4: 运行 ─────────────────────────────────────────
FROM node:22-alpine AS runner

# 配置 apk 使用国内镜像源（使用 HTTP 避免 SSL 问题）
RUN sed -i 's|https://dl-cdn.alpinelinux.org|http://mirrors.aliyun.com|g' /etc/apk/repositories

# tini — 轻量级 init，正确处理 PID 1 和信号转发
# git — Claude Code Agent 需要 git 命令（Bash 工具白名单）
RUN apk add --no-cache tini git bash

# Claude CLI 需要 POSIX shell 环境（无 bash 时启动即失败，报 "No suitable shell found"）
ENV SHELL=/bin/bash

# 配置 npm 使用国内镜像源（使用 HTTP 避免 SSL 问题）
ENV NPM_CONFIG_REGISTRY=http://registry.npmmirror.com

# ─── 安装 Claude Code CLI ─────────────────────────────────
# Agent 功能依赖 @anthropic-ai/claude-agent-sdk，SDK 在运行时需要 Claude Code CLI
# 通过 npm 全局安装，确保 Agent 对话功能在 Docker 容器中正常工作
# 必须执行 postinstall 安装平台原生二进制；--ignore-scripts 会留下无法运行的 CLI 壳。
RUN npm install -g @anthropic-ai/claude-code

# 创建 Claude Code 配置目录（运行时通过 volume 映射宿主机配置）
RUN mkdir -p /root/.claude

WORKDIR /app

# 复制编译产物
COPY --from=builder /app/packages/backend/dist ./dist
COPY --from=builder /app/packages/backend/prisma ./prisma
# 内置 Agent 与外部 Claude 安装共用同一个完整插件，避免工具和 Skills 两套维护。
COPY --from=pruner /app/plugins/githubstars-agent ./agent-plugin
COPY --from=builder /app/package.json ./package.json

# 复制生产依赖（npm prune 后的 node_modules）
COPY --from=builder /app/node_modules ./node_modules

# 复制入口脚本
COPY docker/scripts/entrypoint.sh /usr/local/bin/entrypoint.sh
RUN chmod +x /usr/local/bin/entrypoint.sh

EXPOSE 10002

ENTRYPOINT ["/sbin/tini", "--", "entrypoint.sh"]
